import { DateOfBirthToAgePipe } from './date-of-birth-to-age.pipe';

describe('DateOfBirthToAgePipe', () => {
  it('create an instance', () => {
    const pipe = new DateOfBirthToAgePipe();
    expect(pipe).toBeTruthy();
  });
});
