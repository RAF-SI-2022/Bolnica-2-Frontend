import { PermissionNamesPipePipe } from './permission-names-pipe.pipe';

describe('PermissionNamesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionNamesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
